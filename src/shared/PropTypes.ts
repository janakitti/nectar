export enum Level {
  primary = 'primary',
  secondary = 'secondary',
}

export enum Direction {
  left = 'left',
  right = 'right',
  down = 'down',
  up = 'up',
}

export interface IBreakPoints {
  xs?: number;
  sm?: number;
  md?: number;
  lg?: number;
  xl?: number;
}

export enum BreakPoints {
  xs = 'xs',
  sm = 'sm',
  md = 'md',
  lg = 'lg',
  xl = 'xl',
}
